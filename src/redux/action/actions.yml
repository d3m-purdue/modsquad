setDatasetList:
  - datasets

setActiveDataset:
  - index

setActiveData:
  - data

setVariables:
  - variables

createLogVariable:
  - name
  - data

setExploratoryVar:
  - which
  - var

setModelType:
  - model

setModelInputVars:
  - vars

setModelingVar:
  - which
  - var
